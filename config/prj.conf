# --- ZMK/Zephyr ログを USB CDC に出す（一時的）---
CONFIG_LOG=y
CONFIG_LOG_PRINTK=y
CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_KSCAN_LOG_LEVEL_DBG=y   # 行列スキャンのデバッグログを出力

# 任意（より確実に即時出力したい場合のみ）
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_LOG_DEFAULT_LEVEL=3       # INF 以上（DEBUG まで欲しければ 4）
